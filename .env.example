# Only needed for live runs. `--offline` requires no key at all.
# Any LiteLLM-supported provider works; set the matching key and pass --model.
OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GEMINI_API_KEY=

# CrewAI telemetry is disabled by default in this project.
CREWAI_TRACING_ENABLED=false
